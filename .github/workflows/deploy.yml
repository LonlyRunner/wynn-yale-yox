name: 构建并部署个人网站

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wynn-yale-yox-production
  cancel-in-progress: false

jobs:
  build:
    name: 云端构建
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: 构建后端
        working-directory: backend
        run: mvn -B clean package -DskipTests
      - name: 构建前端
        working-directory: frontend
        run: |
          npm ci
          npm run build
      - name: 判断后端是否需要重启
        id: backend-change
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]] || \
             [[ "${{ github.event.before }}" == "0000000000000000000000000000000000000000" ]] || \
             ! git diff --quiet "${{ github.event.before }}" "${{ github.sha }}" -- backend deploy/wynn-backend.service; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi
      - name: 组装部署包
        run: |
          set -euo pipefail
          mkdir -p .deploy/backend/target .deploy/frontend .deploy/systemd .deploy/certbot-hooks
          cp docker-compose.yml .deploy/
          cp backend/target/wynn-backend-0.1.0.jar .deploy/backend/target/
          cp frontend/Dockerfile.runtime frontend/nginx.conf .deploy/frontend/
          cp -R frontend/dist .deploy/frontend/
          cp deploy/wynn-backend.service .deploy/systemd/
          cp deploy/release.sh .deploy/
          cp deploy/certbot-pre-renew.sh deploy/certbot-post-renew.sh .deploy/certbot-hooks/
          if [[ "${{ steps.backend-change.outputs.changed }}" == "true" ]]; then
            touch .deploy/restart-backend
          fi
          printf '%s' "$DEPLOY_ENV" > .deploy/.env
          test -s .deploy/.env
          mysql_database=$(grep -m1 '^MYSQL_DATABASE=' .deploy/.env | cut -d= -f2- | tr -d '\r')
          mysql_root_password=$(grep -m1 '^MYSQL_ROOT_PASSWORD=' .deploy/.env | cut -d= -f2- | tr -d '\r')
          test -n "$mysql_database"
          test -n "$mysql_root_password"
          printf '\nDB_URL=jdbc:mysql://127.0.0.1:3306/%s?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai\nDB_USERNAME=root\nDB_PASSWORD=%s\n' "$mysql_database" "$mysql_root_password" >> .deploy/.env
          tar -czf deployment.tar.gz -C .deploy .
        env:
          DEPLOY_ENV: ${{ secrets.DEPLOY_ENV }}
      - uses: actions/upload-artifact@v4
        with:
          name: deployment
          path: deployment.tar.gz
          if-no-files-found: error
          retention-days: 3

  deploy:
    name: 发布到阿里云
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: deployment
      - name: 配置 SSH 密钥
        run: |
          install -d -m 700 ~/.ssh
          printf '%s' "$DEPLOY_SSH_PRIVATE_KEY_B64" | base64 --decode > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
        env:
          DEPLOY_SSH_PRIVATE_KEY_B64: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY_B64 }}
      - name: 上传部署包
        run: |
          scp -o StrictHostKeyChecking=accept-new deployment.tar.gz \
            "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/wynn-yale-yox-${GITHUB_SHA}.tar.gz"
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      - name: 重启服务并检查健康状态
        run: |
          ssh -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "set -e; \
             mkdir -p /opt/wynn-yale-yox; \
             tar -xzf /tmp/wynn-yale-yox-${GITHUB_SHA}.tar.gz -C /opt/wynn-yale-yox; \
             rm -f /tmp/wynn-yale-yox-${GITHUB_SHA}.tar.gz; \
             cd /opt/wynn-yale-yox; \
             bash release.sh"
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
      - name: 检查公网 HTTPS（备案完成后应通过）
        continue-on-error: true
        run: |
          curl --fail --silent --show-error --retry 6 --retry-delay 5 \
            https://wynnyaleyox.me/api/public/profile >/dev/null
