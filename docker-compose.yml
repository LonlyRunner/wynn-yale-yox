services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    command: --innodb-buffer-pool-size=256M --max-connections=40
    volumes: [mysql-data:/var/lib/mysql]
    healthcheck: { test: ["CMD","mysqladmin","ping","-h","localhost"], interval: 10s, timeout: 5s, retries: 10 }
  backend:
    build: ./backend
    restart: unless-stopped
    depends_on: { mysql: { condition: service_healthy } }
    environment:
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      DB_USERNAME: root
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      OSS_ENDPOINT: ${OSS_ENDPOINT}
      OSS_BUCKET: ${OSS_BUCKET}
      OSS_ACCESS_KEY_ID: ${OSS_ACCESS_KEY_ID}
      OSS_ACCESS_KEY_SECRET: ${OSS_ACCESS_KEY_SECRET}
      QWEN_BASE_URL: ${QWEN_BASE_URL}
      QWEN_API_KEY: ${QWEN_API_KEY}
      QWEN_IMAGE_MODEL: ${QWEN_IMAGE_MODEL:-qwen-image-3.0}
      QWEN_VISION_MODEL: ${QWEN_VISION_MODEL:-qwen3-vl-flash}
      RELAY_BASE_URL: ${RELAY_BASE_URL}
      RELAY_API_KEY: ${RELAY_API_KEY}
      RELAY_IMAGE_MODEL: ${RELAY_IMAGE_MODEL:-grok-imagine-image-2.0}
      RELAY_VIDEO_MODEL: ${RELAY_VIDEO_MODEL:-grok-imagine-video-1.5}
    deploy: { resources: { limits: { memory: 700M } } }
  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on: [backend]
    ports: ["80:80"]
    deploy: { resources: { limits: { memory: 96M } } }
volumes:
  mysql-data:
